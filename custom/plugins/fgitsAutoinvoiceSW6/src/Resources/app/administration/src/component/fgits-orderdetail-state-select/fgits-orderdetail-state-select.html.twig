{% block fgits_orderdetail_state_select %}
<sw-multi-select
    v-if="options.length"
    :label="$tc('fgits-autoinvoice.state.select.label')"
    :helpText="$tc('fgits-autoinvoice.state.select.helpText')"
    :options="options"
    :isLoading="isLoading"
    valueProperty="id"
    labelProperty="name"
    v-model="currentValue"
>
</sw-multi-select>
{% endblock %}
