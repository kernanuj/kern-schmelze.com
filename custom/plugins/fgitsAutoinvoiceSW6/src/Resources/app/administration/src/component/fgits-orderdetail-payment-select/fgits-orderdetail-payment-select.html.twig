{% block fgits_orderdetail_payment_select %}
<sw-multi-select
    v-if="options.length"
    :label="$tc('fgits-autoinvoice.payment.select.label')"
    :helpText="$tc('fgits-autoinvoice.payment.select.helpText')"
    :options="options"
    :isLoading="isLoading"
    valueProperty="id"
    labelProperty="name"
    v-model="currentValue"
>
</sw-multi-select>
{% endblock %}
